# ---------------------------------------------------------------------------------
# (ALIASES)

alias c="clear"
alias q=" exit"
alias r='exec zsh' # reload

alias rm="rm -I" # asks if folder rec or more than 3 files

alias n="nvim"

alias j="just"
