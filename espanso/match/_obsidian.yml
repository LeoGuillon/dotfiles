matches:
  # - trigger: ":chess"
  #   replace: "```chesser\npgn: |\n\t1. $|$```"

  - trigger: ":data"
    replace: |
      ```dataview
      $|$
      ```

  - trigger: ":list"
    replace: |
      ```dataview
      list
      from $|$
      ```

  - trigger: ":table"
    replace: |
      ```dataview
      table without id $|$
      from
      ```

  - trigger: ":toc"
    replace: |
      ```table-of-contents
      title: ## Sommaire
      minLevel: 2
      ```

  - trigger: ":tasks"
    replace: |
      ```tasks
      $|$
      ```

  - trigger: ":tp"
    replace: "<% tp.$|$ %>"
