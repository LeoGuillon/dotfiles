matches:
  # - trigger: ":chess"
  #   replace: "```chesser\npgn: |\n\t1. $|$```"

  - trigger: ":cursor"
    replace: "<% tp.file.cursor() %>"

  - trigger: ":data"
    replace: |
      ```dataview
      $|$
      ```

  - trigger: ":list"
    replace: |
      ```dataview
      list
      from $|$
      ```

  - trigger: ":table"
    replace: |
      ```dataview
      table without id $|$
      from
      ```

  - trigger: ":toc"
    replace: |
      ```table-of-contents
      title: ## Sommaire
      minLevel: 1
      ```

  - trigger: ":tasks"
    replace: |
      ```tasks
      $|$
      ```

  - trigger: ":tp"
    replace: "<% tp.$|$ %>"
